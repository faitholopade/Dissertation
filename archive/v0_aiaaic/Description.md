For the AIAAIC incidents, I’ve programmatically filtered the full 2,121-row spreadsheet to a small slice of incidents involving public-sector employment or access to benefits, plus a narrower slice where LLM / generative AI is explicitly mentioned.

I’ve saved 10 incidents from each slice into CSVs for manual annotation with my schema.